events {
    worker_connections 1024;
}

stream {
    upstream mysql_backend {
        server mysql-master:3306 weight=1;
        server mysql-slave:3306 weight=1;
    }
    
    server {
        listen 3306;
        proxy_pass mysql_backend;
        proxy_connect_timeout 1s;
    }
}
